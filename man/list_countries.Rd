% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countries.R
\name{list_countries}
\alias{list_countries}
\title{Get a list of countries from the countries resource.}
\usage{
list_countries(
  providers_id = NULL,
  parameters_id = NULL,
  order_by = NULL,
  sort_order = NULL,
  limit = NULL,
  page = NULL,
  as_data_frame = TRUE,
  dry_run = FALSE,
  rate_limit = FALSE,
  api_key = NULL
)
}
\arguments{
\item{providers_id}{A numeric vector of length 1 or more, containing the
ID(s) of the providers to use for filtering results. If multiple IDs are
provided, results matching any of the IDs will be returned.}

\item{parameters_id}{A numeric vector of length 1 or more, containing the
ID(s) of the parameters to use for filtering results. If multiple IDs are
provided, results matching any of the IDs will be returned.}

\item{order_by}{A string.}

\item{sort_order}{A string.}

\item{limit}{An integer.}

\item{page}{An integer.}

\item{as_data_frame}{A logical for toggling whether to return results as
data frame or list default is \code{TRUE}.}

\item{dry_run}{A logical for toggling a dry run of the request, default is
\code{FALSE}.}

\item{rate_limit}{A logical for toggling automatic rate limiting based on
rate limit headers, default is \code{FALSE}.}

\item{api_key}{A valid OpenAQ API key string, default is \code{NULL}.}
}
\value{
A data frame or a list of the results.
}
\description{
Get a list of countries from the countries resource.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
countries <- list_countries()
\dontshow{\}) # examplesIf}
}
