# Toggles on the RATE_LIMIT environment variable to TRUE.

Toggles on the RATE_LIMIT environment variable to TRUE.

## Usage

``` r
enable_rate_limit()
```

## Value

No return value, called for side effects.

## Examples

``` r
enable_rate_limit()
```
